@using DocumentFormat.OpenXml.Office.CustomUI
@model dynamic

@{
    ViewBag.Title = "Category";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container" style="max-width: 900px; margin: 40px auto; background: #fff; padding: 40px 30px; border-radius: 20px; box-shadow: 0 8px 30px rgba(0,0,0,0.08);">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0" style="font-weight: 700; color: #4f46e5;">📁 Danh mục sản phẩm</h2>
        @Html.ActionLink("➕ Tạo mới", "Create", "Category", new { @class = "btn btn-primary rounded-pill px-4 py-2", style = "background: #4f46e5; border: none;" })
    </div>
    @if (ViewBag.Loi != null)
    {
        <div class="alert alert-info text-center mb-4">
            @ViewBag.Loi
        </div>
    }
    <div class="table-responsive">
        <table class="table table-hover align-middle text-center" style="border-radius: 15px; overflow: hidden;">
            <thead style="background: #4f46e5; color: #fff;">
                <tr>
                    <th style="width: 120px;">ID Phân loại</th>
                    <th>Tên phân loại</th>
                    <th>Số lượng sản phẩm<th/>
                    <th style="width: 200px;">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr style="background: #f8fafc;">
                        <td style="font-weight: 600; color: #6366f1;">@Html.Raw(item?.IDCate)</td>
                        <td style="font-size: 1.1em;">@Html.Raw(item?.NameCate)</td>
                        <td style="font-weight: 600; color: #6366f1;">@Html.Raw(item?.TotalProduct)</td>
                        <td></td>
                        <td>
                            <div class="d-flex justify-content-center gap-2">
                                @Html.ActionLink("✏️ Sửa", "Edit", new { Id =(int)item.Id}, new { @class = "btn btn-success rounded-pill px-3 py-1" })
                                @Html.ActionLink("🗑️ Xóa", "Delete", new { Id =(int)item.Id }, new { @class = "btn btn-danger rounded-pill px-3 py-1" })
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>